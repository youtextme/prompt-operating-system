---
name: prompt-os
description: >-
  Alias of /letscook (Prompt OS + JIT Board). Prefer skill letscook — ALWAYS
  auto-applied for objectives. Use when user says /prompt-os or /pos.
---

# /prompt-os

Load and obey the `letscook` skill (same objective runner + JIT Board).

Resolve: `skills/letscook/SKILL.md` → `~/.agents/skills/letscook/SKILL.md` → `~/.cursor/skills/letscook/SKILL.md`.
