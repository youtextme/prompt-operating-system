{
  "name": "prompt-operating-system",
  "version": "3.3.0",
  "description": "Universal Prompt OS — one install, every IDE and CLI routes through objective contracts and machine-enforced evidence. v3.3 cook+status+trust lines, Outcome Frame gate, skip promptOS bypass.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "pos": "./bin/pos.mjs"
  },
  "scripts": {
    "install:local": "node install.mjs",
    "doctor": "node bin/pos.mjs doctor",
    "test": "node kernel/lib/status-law.test.mjs && node kernel/enforce/inject.test.mjs && node kernel/scripts/evidence-check.test.mjs && node kernel/scripts/watchdog.mjs --self-test && node kernel/scripts/possandbox.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "optionalDependencies": {
    "playwright": "^1.52.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/youtextme/prompt-operating-system.git"
  },
  "homepage": "https://youtextme.github.io/prompt-operating-system/"
}
